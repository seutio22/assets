VERIFICAR NO RAILWAY DASHBOARD:

BACKEND:
- Settings → Root Directory: "backend"
- Settings → Build Command: (deixar vazio, usar nixpacks.toml)
- Settings → Start Command: (deixar vazio, usar nixpacks.toml)

FRONTEND:
- Settings → Root Directory: "frontend"
- Settings → Build Command: (deixar vazio, usar nixpacks.toml)
- Settings → Start Command: (deixar vazio, usar nixpacks.toml)

Se não estiver configurado, o Railway não vai fazer deploy automático!

