// Rota desabilitada - modelo OpcaoCampo não existe no schema Prisma
// TODO: Criar modelo OpcaoCampo no schema ou remover esta rota
import { Router } from 'express';

const router = Router();

// Rotas vazias até que o modelo seja criado
export { router as opcaoCampoRoutes };
